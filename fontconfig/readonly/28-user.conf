<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>

  <!-- ## Load per-user customization files. ## -->
  <!-- These should be stored in XDG Base Directory specification
       compliant places. it should be usually:
          $HOME/.config/fontconfig/infinality.d
          $HOME/.config/fontconfig/infinality.conf -->

  <include ignore_missing="yes" prefix="xdg">fontconfig/infinality.d</include>
  <include ignore_missing="yes" prefix="xdg">fontconfig/infinality.conf</include>

</fontconfig>
